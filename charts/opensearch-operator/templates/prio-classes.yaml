apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: high-prio-1000
value: 1000
globalDefault: false  # optional, if true this becomes default for pods without priorityClassName
description: "High priority pods for important workloads"  # optional but recommended
preemptionPolicy: PreemptLowerPriority
